{
  "name": "ussd-flow-engine",
  "version": "1.0.0",
  "scripts": {
    "test:pact": "jest --testPathPattern=pact",
    "pact:publish": "node scripts/pact-broker.js publish",
    "can-i-deploy": "node scripts/pact-broker.js can-i-deploy",
    "record-deployment": "node scripts/pact-broker.js record-deployment"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@pact-foundation/pact": "^16.3.0",
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testTimeout": 30000
  }
}
